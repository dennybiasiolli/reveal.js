#### Fallback Content

Default value for a slot content

```html
<button type="submit">
  <slot>
    Submit <!-- fallback content -->
  </slot>
</button>
```

Rendered only when no content is provided


<aside class="notes">
</aside>
